/* Exercise #1
    source: https://app.rocketseat.com.br/discover/course/node-js-o-motor-da-nave/iniciando-na-pratica/apresentacao-do-desafio
    - Create two JavaScript files;
    - First one exports a function called "getFlag()";
    - "getFlag()" receives a string type argument, and:
        -- Search within "process.argv" array the flag;
        -- Return the value of the flag.

    - Second one imports the function;
    - And inputs the flag for the first one.

    - Run the second file in the terminal;
    - Pass the flags --firstname --lastname;
    - Print in the console the full name and a greeting.
*/
/* const getFlag = require('./exports')
console.log(`
    Hi ${getFlag('--firstname')} ${getFlag('--lastname')}, how are you doing?
`) */